{
  "name": "socialvault",
  "version": "0.1.0",
  "private": true,
  "description": "SocialVault - Secure marketplace for social media accounts",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter '*' dev",
    "build": "pnpm --filter '*' build",
    "lint": "pnpm --filter '*' lint",
    "test": "pnpm --filter '*' test",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\""
  },
  "devDependencies": {
    "prettier": "^3.1.1"
  }
}
