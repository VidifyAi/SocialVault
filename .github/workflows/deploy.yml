name: Deploy to Azure

on:
  push:
    branches:
      - main

env:
  ACR_NAME: ${{ secrets.ACR_NAME }}
  BACKEND_WEBAPP: ${{ secrets.BACKEND_WEBAPP }}
  FRONTEND_WEBAPP: ${{ secrets.FRONTEND_WEBAPP }}
  AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
  AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
  AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        uses: azure/login@v1
        with:
          client-id: ${{ env.AZURE_CLIENT_ID }}
          tenant-id: ${{ env.AZURE_TENANT_ID }}
          subscription-id: ${{ env.AZURE_SUBSCRIPTION_ID }}

      - name: Login to ACR
        run: az acr login --name $ACR_NAME

      - name: Build and push backend
        run: |
          docker build -f apps/backend/Dockerfile -t $ACR_NAME.azurecr.io/backend:${{ github.sha }} .
          docker push $ACR_NAME.azurecr.io/backend:${{ github.sha }}

      - name: Build and push frontend
        run: |
          docker build -f apps/frontend/Dockerfile \
            --build-arg NEXT_PUBLIC_API_URL=https://$BACKEND_WEBAPP.azurewebsites.net \
            -t $ACR_NAME.azurecr.io/frontend:${{ github.sha }} .
          docker push $ACR_NAME.azurecr.io/frontend:${{ github.sha }}

      - name: Deploy and restart backend
        run: |
          az webapp config container set \
            --name $BACKEND_WEBAPP \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
            --docker-custom-image-name $ACR_NAME.azurecr.io/backend:${{ github.sha }} \
            --docker-registry-server-url https://$ACR_NAME.azurecr.io
          az webapp restart --name $BACKEND_WEBAPP --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }}

      - name: Deploy and restart frontend  
        run: |
          az webapp config container set \
            --name $FRONTEND_WEBAPP \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
            --docker-custom-image-name $ACR_NAME.azurecr.io/frontend:${{ github.sha }} \
            --docker-registry-server-url https://$ACR_NAME.azurecr.io
          az webapp restart --name $FRONTEND_WEBAPP --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }}

      - name: Wait and verify backend
        run: |
          echo "Waiting 45s for backend startup..."
          sleep 45
          curl -f -sS https://$BACKEND_WEBAPP.azurewebsites.net/health || echo "Backend check failed but continuing"

      - name: Wait and verify frontend
        run: |
          echo "Waiting 45s for frontend startup..."
          sleep 45
          curl -f -sS -I https://$FRONTEND_WEBAPP.azurewebsites.net || echo "Frontend check failed but continuing"
